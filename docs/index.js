let endAudio,errorAudio,incorrectAudio,correctAudio;loadAudios();const AudioContext=window.AudioContext||window.webkitAudioContext;const audioContext=new AudioContext();let answer=null;let catCounter=0;let allVoices=[];function loadConfig(){if(localStorage.getItem('darkMode')==1){document.documentElement.dataset.theme='dark';}
if(localStorage.getItem('voice')!=1){document.getElementById('voiceOn').classList.add('d-none');document.getElementById('voiceOff').classList.remove('d-none');}}
loadConfig();function toggleDarkMode(){if(localStorage.getItem('darkMode')==1){localStorage.setItem('darkMode',0);delete document.documentElement.dataset.theme;}else{localStorage.setItem('darkMode',1);document.documentElement.dataset.theme='dark';}}
function playAudio(audioBuffer,volume){const audioSource=audioContext.createBufferSource();audioSource.buffer=audioBuffer;if(volume){const gainNode=audioContext.createGain();gainNode.gain.value=volume;gainNode.connect(audioContext.destination);audioSource.connect(gainNode);audioSource.start();}else{audioSource.connect(audioContext.destination);audioSource.start();}}
function unlockAudio(){audioContext.resume();}
function loadAudio(url){return fetch(url).then(response=>response.arrayBuffer()).then(arrayBuffer=>{return new Promise((resolve,reject)=>{audioContext.decodeAudioData(arrayBuffer,(audioBuffer)=>{resolve(audioBuffer);},(err)=>{reject(err);});});});}
function loadAudios(){promises=[loadAudio('mp3/end.mp3'),loadAudio('mp3/cat.mp3'),loadAudio('mp3/incorrect1.mp3'),loadAudio('mp3/correct3.mp3'),];Promise.all(promises).then(audioBuffers=>{endAudio=audioBuffers[0];errorAudio=audioBuffers[1];incorrectAudio=audioBuffers[2];correctAudio=audioBuffers[3];});}
function loadVoices(){const allVoicesObtained=new Promise(function(resolve,reject){let voices=speechSynthesis.getVoices();if(voices.length!==0){resolve(voices);}else{speechSynthesis.addEventListener("voiceschanged",function(){voices=speechSynthesis.getVoices();resolve(voices);});}});allVoicesObtained.then(voices=>{allVoices=voices;addLangRadioBox();});}
loadVoices();function speak(text){speechSynthesis.cancel();const msg=new SpeechSynthesisUtterance(text);const lang=document.getElementById('langRadio').lang.value;const voices=allVoices.filter(voice=>voice.lang==lang);msg.voice=voices[Math.floor(Math.random()*voices.length)];msg.lang='en-US';speechSynthesis.speak(msg);return msg;}
function addLangRadioBox(){const radio=document.getElementById('langRadio');allVoices.forEach((voice,i)=>{const div=document.createElement('div');div.className='form-check form-check-inline';const input=document.createElement('input');input.className='form-check-input';input.name='lang';input.type='radio';input.id='radio'+i;input.value=voice.lang;const label=document.createElement('label');label.className='from-check-label';label.for='radio'+i;label.textContent=voice.lang;div.appendChild(input);div.appendChild(label);radio.appendChild(div);if(voice.lang=='en-US'){input.checked=true;}});}
function getRandomInt(min,max){min=Math.ceil(min);max=Math.floor(max);return Math.floor(Math.random()*(max-min)+min);}
function hideAnswer(){document.getElementById('reply').textContent='';}
function showAnswer(){const msg=speak(answer);msg.onstart=function(){voiceInput.stop();}
msg.onend=async function(){nextProblem();}
document.getElementById('reply').textContent=answer;}
function nextProblem(){hideAnswer();const grade=document.getElementById('grade').selectedIndex+1;const max=Math.pow(10,grade)-1;answer=getRandomInt(0,max).toString();speak(answer);}
function catNyan(){playAudio(errorAudio);}
function catWalk(freq){const area=document.getElementById('catsWalk');const width=area.offsetWidth;const height=area.offsetHeight;const img=document.createElement('img');const imgSize=128;img.src='kohacu.webp';img.style.position='absolute';img.width=imgSize;img.height=imgSize;img.zIndex=1;img.style.top=getRandomInt(0,height-imgSize)+'px';img.style.left=width-imgSize+'px';img.addEventListener('click',function(){catCounter+=1;speak(catCounter);this.remove();},{once:true});area.appendChild(img);const timer=setInterval(function(){const x=parseInt(img.style.left)-1;if(x>-imgSize){img.style.left=x+'px';}else{clearInterval(timer);img.remove();}},freq);}
function catsWalk(freq){const timer=setInterval(function(){if(Math.random()>0.995){catWalk(getRandomInt(5,20));}},10);}
catsWalk(100);let gameTimer;function startGameTimer(){clearInterval(gameTimer);const timeNode=document.getElementById('time');timeNode.innerText='180秒 / 180秒';gameTimer=setInterval(function(){const arr=timeNode.innerText.split('秒 /');const t=parseInt(arr[0]);if(t>0){timeNode.innerText=(t-1)+'秒 /'+arr[1];}else{clearInterval(gameTimer);playAudio(endAudio);playPanel.classList.add('d-none');scorePanel.classList.remove('d-none');}},1000);}
let countdownTimer;function countdown(){clearTimeout(countdownTimer);gameStart.classList.remove('d-none');playPanel.classList.add('d-none');scorePanel.classList.add('d-none');const counter=document.getElementById('counter');counter.innerText=3;countdownTimer=setInterval(function(){const colors=['skyblue','greenyellow','violet','tomato'];if(parseInt(counter.innerText)>1){const t=parseInt(counter.innerText)-1;counter.style.backgroundColor=colors[t];counter.innerText=t;}else{clearTimeout(countdownTimer);gameStart.classList.add('d-none');playPanel.classList.remove('d-none');document.getElementById('score').innerText=0;nextProblem();startGameTimer();}},1000);}
function initCalc(){const replyObj=document.getElementById('reply');const scoreObj=document.getElementById('score');document.getElementById('be').onclick=function(){const reply=replyObj.innerText;if(answer==reply){playAudio(correctAudio);replyObj.innerText='';scoreObj.innerText=parseInt(scoreObj.innerText)+1;nextProblem();}else{playAudio(incorrectAudio);}}
document.getElementById('bc').onclick=function(){replyObj.innerText='';}
for(let i=0;i<10;i++){document.getElementById('b'+i).onclick=function(){const reply=replyObj.innerText;reply+=this.getAttribute('id').slice(-1);replyObj.textContent=reply.slice(0,8);if(answer==reply){playAudio(correctAudio);replyObj.innerText='';scoreObj.innerText=parseInt(scoreObj.innerText)+1;nextProblem();}else if(answer.slice(0,reply.length)!=reply){playAudio(incorrectAudio);}}}}
initCalc();document.addEventListener('click',unlockAudio,{once:true,useCapture:true});